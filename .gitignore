node_modules/
*.log
.env
.env.local
.env.test
*.db
*.db-journal
*.db-wal
*.db-shm
.DS_Store
dist/
build/
coverage/
.iri-tmp/

# `npm run chat-ui:pack` writes the consumer tarball here.
*.tgz

# UI build output and Astro's generated type cache. `dist/` above already
# covers ui/dist; these are the ones it does not.
.astro/
ui/.astro/
